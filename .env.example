# Skip specific pre-commit hooks during local development
# Uncomment and set to skip individual hooks:
# SKIP=ruff,ruff-format

# Skip all pre-commit hooks (use sparingly):
# SKIP=*

# CI pre-commit bypass (for emergency deployments):
# PRE_COMMIT_BYPASS=1
